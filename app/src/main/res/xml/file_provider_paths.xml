<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Downloads are written to the public Downloads directory (see
         DownloadHandler.enqueue's setDestinationInExternalPublicDir), so
         that's the only root FileProvider needs to expose. -->
    <external-path name="downloads" path="Download/" />
</paths>
