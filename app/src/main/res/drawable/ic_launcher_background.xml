<?xml version="1.0" encoding="utf-8"?>
<!-- Solid near-black fill matching the uploaded Knot logo's background,
     so the adaptive icon's background layer is indistinguishable from
     the source art regardless of which mask shape a launcher applies. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:pathData="M0,0h108v108h-108z"
        android:fillColor="#0D0D0F" />
</vector>
